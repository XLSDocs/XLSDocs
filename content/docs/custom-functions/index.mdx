---
title: Custom Functions
description: Name intermediate values and build reusable, custom functions without VBA.
---

Two functions that let a formula do more than compute one thing: LET
names intermediate values inside a single formula, and LAMBDA builds
an entire reusable, custom function out of one — no VBA, no
macro-enabled workbook, and it works in Excel for the web.

<Cards className="grid-cols-3">
  <Card title="LET" description="Assign names to intermediate values within a formula." href="/docs/custom-functions/let" />
  <Card title="LAMBDA" description="Build a custom, reusable function out of a formula." href="/docs/custom-functions/lambda" />
</Cards>
