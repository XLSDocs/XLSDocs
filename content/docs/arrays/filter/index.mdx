---
title: FILTER
description: Return only the rows or columns of a range that match a condition, spilling the results automatically.
---

Return only the rows (or columns) of a range that match a condition —
no helper columns, no array-entered formulas, no manual filtering. The
matching rows spill automatically into the cells below the formula.

<FunctionNav />

export const quickAnswerFacts = [
  { text: 'Live formula — updates automatically', type: 'check' },
  { text: 'Returns multiple rows, not just one', type: 'check' },
  { text: 'Excel 365 & 2021 only', type: 'warn' },
];

<QuickAnswer
  formula={'=FILTER(array, include, [if_empty])'}
  facts={quickAnswerFacts}
/>

## Syntax

<ExcelCode>{`=FILTER(array, include, [if_empty])`}</ExcelCode>

## Parameters

export const parameters = [
  {
    name: 'array',
    type: 'range',
    required: true,
    description: 'The range or array to filter.',
  },
  {
    name: 'include',
    type: 'boolean array',
    required: true,
    description:
      'A boolean array the same height (or width) as `array` — `TRUE` keeps the row, `FALSE` drops it. Usually a comparison like `Sales[Region]="West"`.',
  },
  {
    name: 'if_empty',
    type: 'any',
    required: false,
    description: 'Value to return if no rows match. Without it, a zero-match result throws `#CALC!`.',
  },
];

<ParametersTable items={parameters} />

## Return value

A dynamic array of every row (or column) from `array` where `include`
is `TRUE`, in original order, spilled into the cells below/right of the
formula.

## Description

FILTER is a dynamic array function — it returns a spill range rather
than a single value, and the result updates live as the source data
changes, without re-entering the formula. It replaces the classic
combination of helper columns plus a manual filter.

<Callout type="warn">
  FILTER requires **Excel 365 or Excel 2021**, like other dynamic array
  functions. It is not available in Excel 2019, 2016, or earlier —
  those versions will show `#NAME?`.
</Callout>

## Common errors

### #CALC! — no rows match

<ExcelCode>{`FILTER(A2:C10, B2:B10="Zzz") → #CALC!`}</ExcelCode>

No rows in `array` satisfy `include`, and `if_empty` wasn't
provided. Add a third argument for a fallback value instead:

<ExcelCode>{`FILTER(A2:C10, B2:B10="Zzz", "No matches")`}</ExcelCode>

### #VALUE! — mismatched sizes

<ExcelCode>{`FILTER(A2:C10, B2:B5="West") → #VALUE!`}</ExcelCode>

`include` must be the same height (or width) as `array` — here one
is 9 rows and the other is 4.

### #SPILL! — blocked spill range

Cells below the formula already contain data. Clear the spill range,
or move the formula somewhere clear.

### Combining multiple conditions

<ExcelCode>{`FILTER(A2:C10, (B2:B10="West")*(C2:C10="Q4"))`}</ExcelCode>

Multiply boolean arrays together for AND logic, or add them for OR —
`AND()`/`OR()` don't work element-wise across an array this way.

<Callout type="info">
  To combine multiple conditions inside `include`, multiply boolean
  arrays together for AND logic (`(cond1)*(cond2)`), or add them for OR
  logic (`(cond1)+(cond2)`) — the `AND()`/`OR()` functions don't work
  element-wise across an array the way `*`/`+` do here.
</Callout>

export const faqs = [
  {
    question: 'Why does FILTER return #CALC!?',
    answer:
      "No rows in `array` satisfy `include`. Add a third argument (`if_empty`) to return a fallback value instead of an error.",
  },
  {
    question: 'How do I filter on multiple conditions (AND/OR)?',
    answer:
      'Multiply boolean arrays for AND (`(B2:B10="West")*(C2:C10="Q4")`), or add them for OR (`(B2:B10="West")+(B2:B10="East")`).',
  },
  {
    question: "Can I sort FILTER's results?",
    answer:
      'Yes — wrap the whole formula in SORT or SORTBY: `=SORT(FILTER(A2:C10, B2:B10="West"))`.',
  },
  {
    question:
      "Why use FILTER instead of a helper column and Excel's built-in Filter?",
    answer:
      "FILTER is a live formula — it updates automatically as source data changes, and other formulas can reference its spilled results directly. The built-in Filter/AutoFilter only hides rows visually and doesn't recalculate anything.",
  },
];

<Faq items={faqs} />

## Working with FILTER from code

<CodeBlockTabs defaultValue="Excel">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="Excel">Excel</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="VBA">VBA</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="Python">Python</CodeBlockTabsTrigger>
  </CodeBlockTabsList>
  <CodeBlockTab value="Excel">
    <ExcelCode>{`=FILTER(Sales[#All], Sales[Region]="West", "No matches")`}</ExcelCode>
  </CodeBlockTab>
  <CodeBlockTab value="VBA">
    ```vb lineNumbers
    Dim result As Variant
    result = Application.WorksheetFunction.Filter( _
      Range("Sales"), Range("Sales[Region]").Value = "West", "No matches")
    ```
  </CodeBlockTab>
  <CodeBlockTab value="Python">
    ```python lineNumbers
    import pandas as pd

    df = pd.read_excel("workbook.xlsx", sheet_name="Sales")
    result = df[df["Region"] == "West"]
    print(result)
    ```
  </CodeBlockTab>
</CodeBlockTabs>

## Compatibility

export const compatibility = [
  { label: '365', supported: true },
  { label: '2021', supported: true },
  { label: '2019', supported: false },
  { label: '2016', supported: false },
  { label: 'Web', supported: true },
];

<Compatibility items={compatibility} />

## Related functions

- SORT — often wrapped around FILTER to sort the filtered results.
  Page coming soon (Arrays category).
- UNIQUE — returns the distinct values from a range, and combines well
  with FILTER for "unique values matching a condition." Page coming
  soon (Arrays category).
- SORTBY — sorts one array using the order of a separate array, useful
  when the sort key isn't in the result itself. Page coming soon
  (Arrays category).
- [XLOOKUP](/docs/lookup/xlookup) — returns a single matching value;
  reach for FILTER instead when more than one row can match.

## Further reading

- [Dynamic Arrays Changed How I Write Formulas](/blog/dynamic-arrays-changed-how-i-write-formulas) —
  how FILTER, SORT, and UNIQUE change the shape of formulas worth writing.
