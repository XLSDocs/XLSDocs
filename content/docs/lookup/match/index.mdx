---
title: MATCH
description: Find a value's position within a range, instead of the value itself.
---

Search a range for a value and return its **position** within that
range, not the value itself. The complement to INDEX (which does the
reverse — position to value): together they form one of Excel's most
flexible lookup patterns.

<FunctionNav />

export const quickAnswerFacts = [
  { text: 'Works in every Excel version', type: 'check' },
  { text: 'Returns a position, not a value', type: 'check' },
  { text: 'match_type defaults to 1 (approximate)', type: 'warn' },
];

<QuickAnswer
  formula={'=MATCH(lookup_value, lookup_array, 0)'}
  facts={quickAnswerFacts}
/>

## Syntax

<ExcelCode>{`=MATCH(lookup_value, lookup_array, [match_type])`}</ExcelCode>

## Parameters

export const parameters = [
  {
    name: 'lookup_value',
    type: 'any',
    required: true,
    description: 'The value to search for.',
  },
  {
    name: 'lookup_array',
    type: 'range',
    required: true,
    description: "A single row or column to search — MATCH doesn't work across a 2D range.",
  },
  {
    name: 'match_type',
    type: 'number',
    required: false,
    description:
      '`1` (default) largest value ≤ `lookup_value`, requires `lookup_array` sorted ascending · `0` exact match, any order · `-1` smallest value ≥ `lookup_value`, requires descending sort.',
  },
];

<ParametersTable items={parameters} />

## Return value

The 1-based position of `lookup_value` within `lookup_array` — a
number, not the matching value. Returns `#N/A` if no match is found.

## Description

MATCH searches `lookup_array` and returns where `lookup_value` sits,
counting from 1. On its own that position number isn't usually
useful — it becomes powerful wrapped inside INDEX, which turns "where
is it" into "what's there", or inside functions like VLOOKUP's
`col_index_num` to look up a column by header name instead of a
hardcoded number.

<Callout type="warn">
  `match_type` defaults to `1` (approximate match, requires ascending
  sort) if omitted — the same kind of default-argument trap as
  VLOOKUP's `range_lookup`. Most uses of MATCH want an exact match:
  pass `0` explicitly unless you specifically need a sorted,
  tiered lookup.
</Callout>

<TryItHeading />

<TryIt
  data={{ apple: '1', banana: '2', mango: '3', orange: '4', grape: '5' }}
  defaultValue="Mango"
  label={'Live preview — =MATCH("{value}", Products!A1:A5, 0)'}
/>

## Common errors

### #N/A — no match found

<ExcelCode>{`MATCH("Zzz", A1:A10, 0) → #N/A`}</ExcelCode>

`lookup_value` doesn't exist anywhere in `lookup_array`.

### Wrong position, no error

<ExcelCode>{`MATCH(85, A1:A10) → returns a plausible-looking but incorrect position`}</ExcelCode>

`match_type` was omitted or left at its default (`1`), and
`lookup_array` isn't sorted ascending. Pass `0` explicitly for an
exact match:

<ExcelCode>{`MATCH(85, A1:A10, 0)`}</ExcelCode>

### #N/A — lookup_array is two-dimensional

<ExcelCode>{`MATCH("Widget", A1:C10, 0) → #N/A`}</ExcelCode>

`lookup_array` must be a single row or a single column — MATCH can't
search across a 2D range.

<Callout type="info">
  MATCH only searches a single row or a single column at a time. To
  search a full 2D table, use two MATCH calls — one for the row, one
  for the column — inside INDEX.
</Callout>

export const faqs = [
  {
    question: 'Why does MATCH return the wrong position instead of #N/A?',
    answer:
      '`match_type` is `1` (the default) and `lookup_array` isn\'t sorted in ascending order. Pass `0` as the third argument to force an exact match — this is the same class of mistake as VLOOKUP\'s `range_lookup` defaulting to `TRUE`.',
  },
  {
    question: 'MATCH returns a number — how do I get the actual value?',
    answer:
      "Wrap it in INDEX: `INDEX(return_range, MATCH(lookup_value, lookup_array, 0))` uses the position MATCH finds to pull the actual value from a different range.",
  },
  {
    question: 'Can MATCH search a 2D range like A1:C10?',
    answer:
      'No — `lookup_array` must be a single row or a single column. For a two-dimensional table, use two separate MATCH calls (one per axis) nested inside INDEX.',
  },
  {
    question: 'Does MATCH support wildcards?',
    answer:
      'Yes, with `match_type` set to `0` — `*` matches any number of characters and `?` matches a single character, the same wildcard rules as VLOOKUP and XLOOKUP.',
  },
];

<Faq items={faqs} />

## Working with MATCH from code

<CodeBlockTabs defaultValue="Excel">
  <CodeBlockTabsList>
    <CodeBlockTabsTrigger value="Excel">Excel</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="VBA">VBA</CodeBlockTabsTrigger>
    <CodeBlockTabsTrigger value="Python">Python</CodeBlockTabsTrigger>
  </CodeBlockTabsList>
  <CodeBlockTab value="Excel">
    <ExcelCode>{`=MATCH("Widget", Products!A:A, 0)`}</ExcelCode>
  </CodeBlockTab>
  <CodeBlockTab value="VBA">
    ```vb lineNumbers
    Dim position As Variant
    position = Application.WorksheetFunction.Match( _
      "Widget", Range("Products!A:A"), 0)
    ```
  </CodeBlockTab>
  <CodeBlockTab value="Python">
    ```python lineNumbers
    import pandas as pd

    df = pd.read_excel("workbook.xlsx", sheet_name="Products")
    match = df.index[df["Name"] == "Widget"]
    position = match[0] + 1 if len(match) else "#N/A"  # +1 for 1-based position
    print(position)
    ```
  </CodeBlockTab>
</CodeBlockTabs>

## Compatibility

export const compatibility = [
  { label: '365', supported: true },
  { label: '2021', supported: true },
  { label: '2019', supported: true },
  { label: '2016', supported: true },
  { label: 'Web', supported: true },
];

<Compatibility items={compatibility} />

## Related functions

- [INDEX](/docs/lookup/index) — turns the position MATCH finds back
  into an actual value; almost always used together.
- [XMATCH](/docs/lookup/xmatch) — the modern replacement, defaulting
  to an exact match instead of an approximate one.
- [XLOOKUP](/docs/lookup/xlookup) — combines searching and returning
  into one function, largely replacing INDEX/MATCH in modern Excel.
- [VLOOKUP](/docs/lookup/vlookup) — a common place to nest MATCH,
  looking up `col_index_num` by header name instead of a fixed
  number.
- [IFERROR](/docs/logical/iferror) — wraps MATCH (or INDEX/MATCH
  together) to catch `#N/A` from a failed search.
